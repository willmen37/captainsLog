import React from 'react'

const Index = () => {
    // {logs.map((log, i)=>{
    //     <li key={i}>
    //     <a href={`/show/${log._id}`}>{log.name}</a>
    //     </li>


    //   })}  
  return (
    <div>
    <nav>
        <a href="/new">New log</a>
    </nav>
    <h1>Index Page</h1>
    
    <ul>
     
    </ul>
    
    </div>
  )
}

export default Index
